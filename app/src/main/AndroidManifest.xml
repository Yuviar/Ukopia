<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin untuk mengakses internet (sudah ada dan benar) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Kueri untuk membuka aplikasi lain seperti Instagram & YouTube (sudah ada dan benar) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <package android:name="com.instagram.android" />
        <package android:name="com.google.android.youtube" />
    </queries>

    <!--
        PERUBAHAN PENTING:
        1. Menambahkan android:usesCleartextTraffic="true" untuk mengizinkan koneksi ke API via http://
        2. Memperbaiki android:name untuk LoginActivity dan RegisterActivity agar sesuai dengan lokasi file Kotlin-nya.
    -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ukopia"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

    <activity
        android:name=".SplashActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name=".MainActivity"
        android:exported="false"
        android:windowSoftInputMode="adjustPan"
        android:configChanges="locale|layoutDirection|screenLayout|screenSize|orientation" />

    <!--
        PERBAIKAN: Path ke Activity Auth sekarang sudah benar semua.
        Sebelumnya: ".LoginActivity" dan ".RegisterActivity"
        Sekarang: "com.example.ukopia.ui.auth.LoginActivity", dll.
    -->
    <activity
        android:name="com.example.ukopia.ui.auth.LoginActivity"
        android:exported="false" />
    <activity
        android:name="com.example.ukopia.ui.auth.RegisterActivity"
        android:exported="false" />
    <activity
        android:name="com.example.ukopia.ui.auth.LupaPasswordActivity"
        android:exported="false" />

</application>

    </manifest>